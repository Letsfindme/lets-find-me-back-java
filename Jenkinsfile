pipeline {
    agent {
         dockerfile true
    }

    stages {
        stage('Clone repository') {
            steps {
                sh 'pwd'
                sh 'ls'
            }
        }

    }

}